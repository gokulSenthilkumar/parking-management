services:
  - type: web
    name: parking-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: APP_CORS_ALLOWED_ORIGINS
        value: http://localhost:5173,https://parknest-pl.netlify.app
    healthCheckPath: /api/settings
